volumes:
  wingz_backend_local_postgres_data: {}
  wingz_backend_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: wingz_backend_local_django
    container_name: wingz_backend_local_django
    depends_on:
      - postgres
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/local/postgres/Dockerfile
    image: wingz_backend_local_postgres
    container_name: wingz_backend_local_postgres
    volumes:
      - wingz_backend_local_postgres_data:/var/lib/postgresql/18/docker
      - wingz_backend_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres
